version: "3"

services:
  nginx:
    image: nginx
    restart: always
    ports:
      - "80:80"
    volumes:
      - ./data/nginx:/etc/nginx/conf.d:ro

  ursa-dev:
    image: mtavano/ursadev:latest
    restart: on-failure
    ports:
      - "4069:4069"
    expose:
      - "4069"

